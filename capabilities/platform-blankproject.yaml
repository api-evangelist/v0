naftiko: 1.0.0-alpha2
info:
  label: v0 App API — blankProject
  description: 'v0 App API — blankProject. 1 operations. Lead operation: blankProject. Self-contained Naftiko capability covering
    one V0 business surface.'
  tags:
  - V0
  - blankProject
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    V0_API_KEY: V0_API_KEY
capability:
  consumes:
  - type: http
    namespace: platform-blankproject
    baseUri: https://api.v0.dev/v1
    description: v0 App API — blankProject business capability. Self-contained, no shared references.
    resources:
    - name: blank-project-start-chat
      path: /blank-project/start-chat
      operations:
      - name: blankprojectstartchatcreate
        method: POST
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.V0_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: platform-blankproject-rest
    port: 8080
    description: REST adapter for v0 App API — blankProject. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/blank-project/start-chat
      name: blank-project-start-chat
      description: REST surface for blank-project-start-chat.
      operations:
      - method: POST
        name: blankprojectstartchatcreate
        description: blankprojectstartchatcreate
        call: platform-blankproject.blankprojectstartchatcreate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: platform-blankproject-mcp
    port: 9090
    transport: http
    description: MCP adapter for v0 App API — blankProject. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: blankprojectstartchatcreate
      description: blankprojectstartchatcreate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: platform-blankproject.blankprojectstartchatcreate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
